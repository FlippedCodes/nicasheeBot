version: '3.3'
services:
  bot_nb_beta:
    build:
      context: ./
    restart: always
    environment:
      BotToken: ${DISCORD_BOT_TOKEN}
      DB_HOST: gurglebot_db_gb_1
      DBName: gurglebot
      DB_user: gurglebot
      DBPassw: ${DB_PASSWORD}

networks:
  default:
    external:
      name: tdmnet
