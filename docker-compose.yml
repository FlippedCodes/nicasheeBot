version: '3.3'
services:
  bot_nb:
    build:
      context: ./
    restart: always
    environment:
      BotToken: ${DISCORD_BOT_TOKEN}
      DBHost: gurglebot_db_gb_1
      DBName: gurglebot
      DB_user: gurglebot
      DBPassw: ${DB_PASSWORD}

networks:
  default:
    external:
      name: tdmnet
